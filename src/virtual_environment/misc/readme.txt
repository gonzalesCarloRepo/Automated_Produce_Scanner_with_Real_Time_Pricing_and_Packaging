These are the python programs that was run inside the raspberry pi 4 model B that boost the productivity and made an efficient impact in dataset creation and training. These are the programs that captures picture using the raspberry pi camera integrated in the rpi 4 in capturing or collecting images of red onions, garlic, and marble potatoe to be used in training the object detection model. 